public interface Flyable {

    // Interfaces only have abstract methods; Abstract methods don't have a body; inside an interface the abstract
    // methods act as a contract for anything that 'implements' the interface.
    public void fly();

}
